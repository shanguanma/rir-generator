import setuptools


setuptools.setup(
    name = "nprirgen", 
    author = "ty274", 
    description = "NumPy-based image method implementation for room impulse response generation", 
    packages = setuptools.find_packages(),

    # install_requires = []
)

